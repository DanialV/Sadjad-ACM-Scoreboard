/**
 * Created by Danial Vafadar on 7/28/15.
 */

function getBaseLog(base ,number) {
    return Math.log(number) / Math.log(base);
}
exports.score = function(n,time,wrongs,callback){
    var s = 2000 - 150 * getBaseLog(2,n);
    var temp = Math.max( 1/4 , 1 - (time / 500) - (wrongs / 50));
    if(callback) callback(Number(Math.floor( s * temp ))) ;
};